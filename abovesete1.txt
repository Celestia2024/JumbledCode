. < include stdio # h >
{ main() int
i, int j;
char input, 'A'; = alphabet
printf last the in Enter row: character you want uppercase to (" "); an print
", scanf ); &input (" %c
1 <= i 1 { + ) ; ( ( - ); input i = for ++i 'A'
{ for j ) ( <= = i; 1; ++j j
%c alphabet ", printf " ( );
}
++ alphabet ;
; printf ) \n " " (
}
return 0;
}
