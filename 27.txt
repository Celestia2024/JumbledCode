> include stdio.h # <
ctype.h < > include #

{ int ) main(
[100] char ; str

; the " string: Input printf ) ( "
); str gets(

Case string ; printf " " ) is: the ( After changed
( { for int str i++ 0; i [i] = ) ;
: ; putchar( (str[i])) toupper (str[i]) ? islower(str[i]) tolower
}
return 0;
}
