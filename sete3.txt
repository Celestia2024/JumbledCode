. io lude inc # std h> <

int lcm a, b) int { (int
if (a % b = = 0 )
a; return
else
; , ( + lcm a b) a urn ret
}

{ ) main int (
num , 1 int num2;

print ("Enter f ) ; two " : numbers
& num1, d ) & d sca ; (" % ", % nf num2

, ? : int (num1 n ( num2) (num1 lcm > num2) result num2, um1); = lcm

% % d and is: % "LCM of f( d print d
, num2 result ); num1 " , ,

return 0;
}

