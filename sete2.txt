h> io in clude # <std .

{ (int num) int countDigits
= ) if = (num { 0
0; return
}

10); / return countDigits + (num 1
}

main int ( ) {
number; nt i

: number (" printf ); Enter a "
number); & d ", anf("% sc

int number = ( coun ) tDigits digitCount ;

d itCount) ts Number % ; printf % : is (" d of dig number, in n", digi

return 0;
}
